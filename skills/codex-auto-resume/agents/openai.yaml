interface:
  display_name: "Codex Auto Resume"
  short_description: "Safely resume eligible Codex tasks after subscription usage resets."
  default_prompt: "Use $codex-auto-resume to preflight this task, keep safe checkpoints, and resume it after included-usage windows reset."
policy:
  allow_implicit_invocation: true
